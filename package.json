{
  "name": "totaren-bot",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "gcloud functions deploy totaren-bot --runtime nodejs10 --trigger-http --allow-unauthenticated --entry-point webhook",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/express": "^4.17.3",
    "dotenv": "^8.2.0",
    "telegraf": "^3.36.0"
  },
  "devDependencies": {
    "prettier": "2.0.5",
    "typescript": "3.9.5"
  }
}
